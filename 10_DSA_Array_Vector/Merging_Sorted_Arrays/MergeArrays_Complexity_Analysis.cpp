#include <iostream>

using namespace std;

int main() {

    // 1. If array A has 5 elements and array B has 5 elements,
    //    how many total elements are we merging?
    // 10 elements

    // 2. If A and B each had 100 elements,
    //    approximately how many elements would the algorithm process?
    // 200 elements

    // 3. If A has n elements and B has m elements,
    //    what do you think the amount of work grows like?
    //
    //    A. O(1)
    //    B. O(log n)
    //    C. O(n + m)
    //    D. O(n²)

    // C. O(n + m)

    // 4. Why isn't this O(n²)?
    // the total number of loop iterations is bounded by n + m

    // 5. What is the Big O time complexity of merging
    //    two sorted arrays?
    // O(n + m)



    return 0;
}